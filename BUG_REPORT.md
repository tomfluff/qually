- Undo/Redo does not work on Text modifications based on AI suggestions.
- AI suggestion (noticings) view toggle should allow to select specific ones to see or not see.
- Adding new code popover/modal needs to use the "Sidebar" text size.
- If user is clicking on code lane and the popover opens, another click on the codelane should close it.
- in video pane, speed can be folded into a popover menu with more items x0.5--x4, play button needs to be better designed (circlular).
- In Zen mode, "Exit zen" should use the "Sidebar" text size.
- In the Browse sidebar, the code names are clipped from the bottom, should have a list similar to the transcript sidebar, there it's not clipped.
- Option to see only specific speaker's transcript (isolate/filter a person's dialogue)
- Making it possible to drag the video panel from top or bottom bars and making it clearer that the video can be dragged
- Making it optional for video looping when editing a quote in the transcript, pause/play for looping, changing the speed
- need to add a setting to control the scroll distance, how much the transcript scrolls when using the scrollwheel
